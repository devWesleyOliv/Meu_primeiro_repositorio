'condição' ? 'retorno se for verdadeira' : 'retorno se for falsa';

let temperature = 35;
let weather = temperature > 30 && 'Tá quente';

console.log(`A temperatura é ${temperature}, portanto, ${weather}.`);